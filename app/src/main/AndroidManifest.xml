<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- 触觉反馈：部分 ROM（如 ColorOS）要求声明该权限才允许 performHapticFeedback 生效 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- miuix-blur 声明 minSdk 33（RuntimeShader 模糊依赖），本应用保留 minSdk 24：
         API 33+ 走真实玻璃模糊，低版本在代码中运行时降级为伪玻璃底栏，不触碰该类库。 -->
    <uses-sdk tools:overrideLibrary="top.yukonga.miuix.kmp.blur" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TempMailApp"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:ignore="TestOnly">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TempMailApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
